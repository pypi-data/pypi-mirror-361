# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class DeviceState(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DeviceState()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDeviceState(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # DeviceState
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # DeviceState
    def Request(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.profinet.device.fbs.DeviceStateRequest import DeviceStateRequest
            obj = DeviceStateRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # DeviceState
    def Response(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.profinet.device.fbs.DeviceStateResponse import DeviceStateResponse
            obj = DeviceStateResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def DeviceStateStart(builder): builder.StartObject(2)
def Start(builder):
    return DeviceStateStart(builder)
def DeviceStateAddRequest(builder, request): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(request), 0)
def AddRequest(builder, request):
    return DeviceStateAddRequest(builder, request)
def DeviceStateAddResponse(builder, response): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(response), 0)
def AddResponse(builder, response):
    return DeviceStateAddResponse(builder, response)
def DeviceStateEnd(builder): return builder.EndObject()
def End(builder):
    return DeviceStateEnd(builder)
import comm.profinet.device.fbs.DeviceStateRequest
import comm.profinet.device.fbs.DeviceStateResponse
try:
    from typing import Optional
except:
    pass

class DeviceStateT(object):

    # DeviceStateT
    def __init__(self):
        self.request = None  # type: Optional[comm.profinet.device.fbs.DeviceStateRequest.DeviceStateRequestT]
        self.response = None  # type: Optional[comm.profinet.device.fbs.DeviceStateResponse.DeviceStateResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        deviceState = DeviceState()
        deviceState.Init(buf, pos)
        return cls.InitFromObj(deviceState)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, deviceState):
        x = DeviceStateT()
        x._UnPack(deviceState)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.request == other.request and \
            self.response == other.response

    # DeviceStateT
    def _UnPack(self, deviceState):
        if deviceState is None:
            return
        if deviceState.Request() is not None:
            self.request = comm.profinet.device.fbs.DeviceStateRequest.DeviceStateRequestT.InitFromObj(deviceState.Request())
        if deviceState.Response() is not None:
            self.response = comm.profinet.device.fbs.DeviceStateResponse.DeviceStateResponseT.InitFromObj(deviceState.Response())

    # DeviceStateT
    def Pack(self, builder):
        if self.request is not None:
            request = self.request.Pack(builder)
        if self.response is not None:
            response = self.response.Pack(builder)
        DeviceStateStart(builder)
        if self.request is not None:
            DeviceStateAddRequest(builder, request)
        if self.response is not None:
            DeviceStateAddResponse(builder, response)
        deviceState = DeviceStateEnd(builder)
        return deviceState
