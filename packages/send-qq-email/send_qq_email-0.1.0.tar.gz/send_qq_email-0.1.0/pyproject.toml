[project]
name = "send-qq-email"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
send-qq-email = "send_qq_email:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
