

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pybakul"
version = "0.1.0"
description = "String utility package with character frequency and string reversal"
authors = [
  { name="Bakul Sarkar", email="your.email@example.com" }
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[project.urls]
"Homepage" = "https://github.com/yourusername/pybakul"
