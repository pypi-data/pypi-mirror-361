[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zynapse10"
version = "0.1.0"
description = "CLI للتفاعل مع Gemini AI من الطرفية"
readme = "README.md"
authors = [{name="Your Name", email="you@example.com"}]
license = {text = "MIT"}
requires-python = ">=3.7"

[project.scripts]
zynapse10 = "zynapse10.cli:main"
