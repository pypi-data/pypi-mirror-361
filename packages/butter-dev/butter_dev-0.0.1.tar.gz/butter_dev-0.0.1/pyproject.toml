[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "butter-dev"
version = "0.0.1"
authors = [
    { name = "Erik Dunteman", email = "erik@butter.dev" },
]
description = "butter"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
]

[tool.hatch.build]
packages = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/butter"] 

# .[dev]

[project.optional-dependencies]
dev = [
    "ruff>=0.3.0",
    "twine",
    "build"
]

[tool.ruff]
include = ["src/butter/**/*.py", "tests/**/*.py"]
target-version = "py37"

# Enable rules
lint.select = [
    "E",  # pycodestyle
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "N",  # pep8-naming
]
lint.ignore = []
lint.dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

line-length = 160

[tool.uv.workspace]
members = ["examples/chat"]
