[project]
name = "reyncher-mcp-demp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "reyncherrr", email = "xhw99493133@foxmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
reyncher-mcp-demp = "reyncher_mcp_demp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
