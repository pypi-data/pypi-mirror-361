[project]
name = "erpnext-mcp-server"
version = "0.1.0"
description = "An MCP server for interacting with an ERPNext instance."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "mcp[cli]>=1.10.1",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "markdownify",
    "readabilipy"
]
