[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry.dependencies]
python = "^3.8"
python-docx = "^0.8.11"
fastmcp = "^0.1.0"
httpx = "^0.24.0"

