# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# information on all available data items
class StateDataItems(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StateDataItems()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStateDataItems(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # StateDataItems
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # all available data items (URI and current value)
    # StateDataItems
    def Items(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from motion.core.fbtypes.DataItem import DataItem
            obj = DataItem()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # StateDataItems
    def ItemsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StateDataItems
    def ItemsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def StateDataItemsStart(builder): builder.StartObject(1)
def Start(builder):
    return StateDataItemsStart(builder)
def StateDataItemsAddItems(builder, items): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(items), 0)
def AddItems(builder, items):
    return StateDataItemsAddItems(builder, items)
def StateDataItemsStartItemsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartItemsVector(builder, numElems):
    return StateDataItemsStartItemsVector(builder, numElems)
def StateDataItemsEnd(builder): return builder.EndObject()
def End(builder):
    return StateDataItemsEnd(builder)
import motion.core.fbtypes.DataItem
try:
    from typing import List
except:
    pass

class StateDataItemsT(object):

    # StateDataItemsT
    def __init__(self):
        self.items = None  # type: List[motion.core.fbtypes.DataItem.DataItemT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stateDataItems = StateDataItems()
        stateDataItems.Init(buf, pos)
        return cls.InitFromObj(stateDataItems)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stateDataItems):
        x = StateDataItemsT()
        x._UnPack(stateDataItems)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.items == other.items

    # StateDataItemsT
    def _UnPack(self, stateDataItems):
        if stateDataItems is None:
            return
        if not stateDataItems.ItemsIsNone():
            self.items = []
            for i in range(stateDataItems.ItemsLength()):
                if stateDataItems.Items(i) is None:
                    self.items.append(None)
                else:
                    dataItem_ = motion.core.fbtypes.DataItem.DataItemT.InitFromObj(stateDataItems.Items(i))
                    self.items.append(dataItem_)

    # StateDataItemsT
    def Pack(self, builder):
        if self.items is not None:
            itemslist = []
            for i in range(len(self.items)):
                itemslist.append(self.items[i].Pack(builder))
            StateDataItemsStartItemsVector(builder, len(self.items))
            for i in reversed(range(len(self.items))):
                builder.PrependUOffsetTRelative(itemslist[i])
            items = builder.EndVector()
        StateDataItemsStart(builder)
        if self.items is not None:
            StateDataItemsAddItems(builder, items)
        stateDataItems = StateDataItemsEnd(builder)
        return stateDataItems
