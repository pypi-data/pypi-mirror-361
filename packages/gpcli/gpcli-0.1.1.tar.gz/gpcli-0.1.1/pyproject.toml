[project]
name = "gpcli"
version = "0.1.1"
description = "Fast git add/commit/push via one CLI command"
authors = [
    {name = "Froas", email = "froas.dev@gmail.com"}
]
readme = "README.md"
license = {file = "LICENSE"}
dependencies = []

[project.scripts]
gp = "pycommit:main"
