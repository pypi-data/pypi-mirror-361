# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

#VoE response
class VoeDataResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VoeDataResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVoeDataResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # VoeDataResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    #Data buffer for read data, include VoE header
    # VoeDataResponse
    def Data(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # VoeDataResponse
    def DataAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # VoeDataResponse
    def DataLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VoeDataResponse
    def DataIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def VoeDataResponseStart(builder): builder.StartObject(1)
def Start(builder):
    return VoeDataResponseStart(builder)
def VoeDataResponseAddData(builder, data): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(data), 0)
def AddData(builder, data):
    return VoeDataResponseAddData(builder, data)
def VoeDataResponseStartDataVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartDataVector(builder, numElems):
    return VoeDataResponseStartDataVector(builder, numElems)
def VoeDataResponseEnd(builder): return builder.EndObject()
def End(builder):
    return VoeDataResponseEnd(builder)
try:
    from typing import List
except:
    pass

class VoeDataResponseT(object):

    # VoeDataResponseT
    def __init__(self):
        self.data = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        voeDataResponse = VoeDataResponse()
        voeDataResponse.Init(buf, pos)
        return cls.InitFromObj(voeDataResponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, voeDataResponse):
        x = VoeDataResponseT()
        x._UnPack(voeDataResponse)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.data == other.data

    # VoeDataResponseT
    def _UnPack(self, voeDataResponse):
        if voeDataResponse is None:
            return
        if not voeDataResponse.DataIsNone():
            if np is None:
                self.data = []
                for i in range(voeDataResponse.DataLength()):
                    self.data.append(voeDataResponse.Data(i))
            else:
                self.data = voeDataResponse.DataAsNumpy()

    # VoeDataResponseT
    def Pack(self, builder):
        if self.data is not None:
            if np is not None and type(self.data) is np.ndarray:
                data = builder.CreateNumpyVector(self.data)
            else:
                VoeDataResponseStartDataVector(builder, len(self.data))
                for i in reversed(range(len(self.data))):
                    builder.PrependUint8(self.data[i])
                data = builder.EndVector()
        VoeDataResponseStart(builder)
        if self.data is not None:
            VoeDataResponseAddData(builder, data)
        voeDataResponse = VoeDataResponseEnd(builder)
        return voeDataResponse
