# automatically generated by the FlatBuffers compiler, do not modify

# namespace: builtin

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ByteString(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ByteString()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsByteString(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ByteString
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ByteString
    def Value(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # ByteString
    def ValueAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # ByteString
    def ValueLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ByteString
    def ValueIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ByteStringStart(builder): builder.StartObject(1)
def Start(builder):
    return ByteStringStart(builder)
def ByteStringAddValue(builder, value): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(value), 0)
def AddValue(builder, value):
    return ByteStringAddValue(builder, value)
def ByteStringStartValueVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartValueVector(builder, numElems):
    return ByteStringStartValueVector(builder, numElems)
def ByteStringEnd(builder): return builder.EndObject()
def End(builder):
    return ByteStringEnd(builder)
try:
    from typing import List
except:
    pass

class ByteStringT(object):

    # ByteStringT
    def __init__(self):
        self.value = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        byteString = ByteString()
        byteString.Init(buf, pos)
        return cls.InitFromObj(byteString)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, byteString):
        x = ByteStringT()
        x._UnPack(byteString)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.value == other.value

    # ByteStringT
    def _UnPack(self, byteString):
        if byteString is None:
            return
        if not byteString.ValueIsNone():
            if np is None:
                self.value = []
                for i in range(byteString.ValueLength()):
                    self.value.append(byteString.Value(i))
            else:
                self.value = byteString.ValueAsNumpy()

    # ByteStringT
    def Pack(self, builder):
        if self.value is not None:
            if np is not None and type(self.value) is np.ndarray:
                value = builder.CreateNumpyVector(self.value)
            else:
                ByteStringStartValueVector(builder, len(self.value))
                for i in reversed(range(len(self.value))):
                    builder.PrependUint8(self.value[i])
                value = builder.EndVector()
        ByteStringStart(builder)
        if self.value is not None:
            ByteStringAddValue(builder, value)
        byteString = ByteStringEnd(builder)
        return byteString
