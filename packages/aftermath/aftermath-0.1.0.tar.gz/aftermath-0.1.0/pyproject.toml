[build-system]
requires=["setuptools", "wheel"]
build-backend="setuptools.build_meta"

[project]
name = "aftermath"
version = "0.1.0"
description = "A lightweight math module with matrix, vector, and combinatoric functions."
authors = [
    { name="pb", email="paulpb0725@gmail.com" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
dependencies = []