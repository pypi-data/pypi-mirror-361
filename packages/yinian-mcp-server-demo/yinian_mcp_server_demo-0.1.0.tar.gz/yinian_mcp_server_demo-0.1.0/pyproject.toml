[project]
name = "yinian-mcp-server-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "泡椒", email = "1062788706@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
yinian-mcp-server-demo = "yinian_mcp_server_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
