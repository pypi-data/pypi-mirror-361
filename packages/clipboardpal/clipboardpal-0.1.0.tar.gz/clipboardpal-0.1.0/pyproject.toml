[project]
name = "clipboardpal"
version = "0.1.0"
description = "An MCP tool to get/set clipboard content"
authors = [{ name = "Your Name", email = "you@example.com" }]
dependencies = ["pyperclip", "mcp"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["clipboardpal"]
