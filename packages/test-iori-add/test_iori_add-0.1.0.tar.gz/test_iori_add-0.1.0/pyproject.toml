[project]
name = "test-iori-add"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
test-iori-add = "test_iori_add:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
