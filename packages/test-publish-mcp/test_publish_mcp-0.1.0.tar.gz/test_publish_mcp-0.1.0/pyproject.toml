[project]
name = "test-publish-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "XDY", email = "examle@examle.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.11.0",
    "openai>=1.95.0",
    "python-dotenv>=1.1.1",
]

[project.scripts]
test-publish-mcp = "test_publish_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
