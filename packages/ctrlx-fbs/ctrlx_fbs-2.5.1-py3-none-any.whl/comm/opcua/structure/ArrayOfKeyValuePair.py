# automatically generated by the FlatBuffers compiler, do not modify

# namespace: structure

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ArrayOfKeyValuePair(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayOfKeyValuePair()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayOfKeyValuePair(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ArrayOfKeyValuePair
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ArrayOfKeyValuePair
    def Array(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from comm.opcua.structure.KeyValuePair import KeyValuePair
            obj = KeyValuePair()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ArrayOfKeyValuePair
    def ArrayLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ArrayOfKeyValuePair
    def ArrayIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ArrayOfKeyValuePairStart(builder): builder.StartObject(1)
def Start(builder):
    return ArrayOfKeyValuePairStart(builder)
def ArrayOfKeyValuePairAddArray(builder, array): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(array), 0)
def AddArray(builder, array):
    return ArrayOfKeyValuePairAddArray(builder, array)
def ArrayOfKeyValuePairStartArrayVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartArrayVector(builder, numElems):
    return ArrayOfKeyValuePairStartArrayVector(builder, numElems)
def ArrayOfKeyValuePairEnd(builder): return builder.EndObject()
def End(builder):
    return ArrayOfKeyValuePairEnd(builder)
import comm.opcua.structure.KeyValuePair
try:
    from typing import List
except:
    pass

class ArrayOfKeyValuePairT(object):

    # ArrayOfKeyValuePairT
    def __init__(self):
        self.array = None  # type: List[comm.opcua.structure.KeyValuePair.KeyValuePairT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayOfKeyValuePair = ArrayOfKeyValuePair()
        arrayOfKeyValuePair.Init(buf, pos)
        return cls.InitFromObj(arrayOfKeyValuePair)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayOfKeyValuePair):
        x = ArrayOfKeyValuePairT()
        x._UnPack(arrayOfKeyValuePair)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.array == other.array

    # ArrayOfKeyValuePairT
    def _UnPack(self, arrayOfKeyValuePair):
        if arrayOfKeyValuePair is None:
            return
        if not arrayOfKeyValuePair.ArrayIsNone():
            self.array = []
            for i in range(arrayOfKeyValuePair.ArrayLength()):
                if arrayOfKeyValuePair.Array(i) is None:
                    self.array.append(None)
                else:
                    keyValuePair_ = comm.opcua.structure.KeyValuePair.KeyValuePairT.InitFromObj(arrayOfKeyValuePair.Array(i))
                    self.array.append(keyValuePair_)

    # ArrayOfKeyValuePairT
    def Pack(self, builder):
        if self.array is not None:
            arraylist = []
            for i in range(len(self.array)):
                arraylist.append(self.array[i].Pack(builder))
            ArrayOfKeyValuePairStartArrayVector(builder, len(self.array))
            for i in reversed(range(len(self.array))):
                builder.PrependUOffsetTRelative(arraylist[i])
            array = builder.EndVector()
        ArrayOfKeyValuePairStart(builder)
        if self.array is not None:
            ArrayOfKeyValuePairAddArray(builder, array)
        arrayOfKeyValuePair = ArrayOfKeyValuePairEnd(builder)
        return arrayOfKeyValuePair
