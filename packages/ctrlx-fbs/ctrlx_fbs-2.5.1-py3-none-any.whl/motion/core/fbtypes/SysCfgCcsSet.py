# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# configuration of a single set for the camera coordinate system
class SysCfgCcsSet(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SysCfgCcsSet()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSysCfgCcsSet(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # SysCfgCcsSet
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # name of the set (required for load/save)
    # SysCfgCcsSet
    def SetName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # XYZ offsets (must be exactly three values)
    # SysCfgCcsSet
    def OffsetXyz(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SysCfgCcsSet
    def OffsetXyzAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SysCfgCcsSet
    def OffsetXyzLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SysCfgCcsSet
    def OffsetXyzIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # rotation offsets as Euler angles ZY'Z'' (must be exactly three values)
    # SysCfgCcsSet
    def Orientation(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SysCfgCcsSet
    def OrientationAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SysCfgCcsSet
    def OrientationLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SysCfgCcsSet
    def OrientationIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # offsets for auxilary axes (must be exactly 10 values)
    # SysCfgCcsSet
    def OffsetAux(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SysCfgCcsSet
    def OffsetAuxAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # SysCfgCcsSet
    def OffsetAuxLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SysCfgCcsSet
    def OffsetAuxIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Units for XYZ offsets (must be exactly three values)
    # SysCfgCcsSet
    def OffsetXyzunits(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # SysCfgCcsSet
    def OffsetXyzunitsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SysCfgCcsSet
    def OffsetXyzunitsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # Units for rotation offsets (must be exactly three values)
    # SysCfgCcsSet
    def OrientationUnits(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # SysCfgCcsSet
    def OrientationUnitsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SysCfgCcsSet
    def OrientationUnitsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # Units for offsets for auxilary axes (must be exactly 10 values)
    # SysCfgCcsSet
    def OffsetAuxUnits(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # SysCfgCcsSet
    def OffsetAuxUnitsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SysCfgCcsSet
    def OffsetAuxUnitsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def SysCfgCcsSetStart(builder): builder.StartObject(7)
def Start(builder):
    return SysCfgCcsSetStart(builder)
def SysCfgCcsSetAddSetName(builder, setName): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(setName), 0)
def AddSetName(builder, setName):
    return SysCfgCcsSetAddSetName(builder, setName)
def SysCfgCcsSetAddOffsetXyz(builder, offsetXyz): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(offsetXyz), 0)
def AddOffsetXyz(builder, offsetXyz):
    return SysCfgCcsSetAddOffsetXyz(builder, offsetXyz)
def SysCfgCcsSetStartOffsetXyzVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartOffsetXyzVector(builder, numElems):
    return SysCfgCcsSetStartOffsetXyzVector(builder, numElems)
def SysCfgCcsSetAddOrientation(builder, orientation): builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(orientation), 0)
def AddOrientation(builder, orientation):
    return SysCfgCcsSetAddOrientation(builder, orientation)
def SysCfgCcsSetStartOrientationVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartOrientationVector(builder, numElems):
    return SysCfgCcsSetStartOrientationVector(builder, numElems)
def SysCfgCcsSetAddOffsetAux(builder, offsetAux): builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(offsetAux), 0)
def AddOffsetAux(builder, offsetAux):
    return SysCfgCcsSetAddOffsetAux(builder, offsetAux)
def SysCfgCcsSetStartOffsetAuxVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartOffsetAuxVector(builder, numElems):
    return SysCfgCcsSetStartOffsetAuxVector(builder, numElems)
def SysCfgCcsSetAddOffsetXyzunits(builder, offsetXyzunits): builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(offsetXyzunits), 0)
def AddOffsetXyzunits(builder, offsetXyzunits):
    return SysCfgCcsSetAddOffsetXyzunits(builder, offsetXyzunits)
def SysCfgCcsSetStartOffsetXyzunitsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartOffsetXyzunitsVector(builder, numElems):
    return SysCfgCcsSetStartOffsetXyzunitsVector(builder, numElems)
def SysCfgCcsSetAddOrientationUnits(builder, orientationUnits): builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(orientationUnits), 0)
def AddOrientationUnits(builder, orientationUnits):
    return SysCfgCcsSetAddOrientationUnits(builder, orientationUnits)
def SysCfgCcsSetStartOrientationUnitsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartOrientationUnitsVector(builder, numElems):
    return SysCfgCcsSetStartOrientationUnitsVector(builder, numElems)
def SysCfgCcsSetAddOffsetAuxUnits(builder, offsetAuxUnits): builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(offsetAuxUnits), 0)
def AddOffsetAuxUnits(builder, offsetAuxUnits):
    return SysCfgCcsSetAddOffsetAuxUnits(builder, offsetAuxUnits)
def SysCfgCcsSetStartOffsetAuxUnitsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartOffsetAuxUnitsVector(builder, numElems):
    return SysCfgCcsSetStartOffsetAuxUnitsVector(builder, numElems)
def SysCfgCcsSetEnd(builder): return builder.EndObject()
def End(builder):
    return SysCfgCcsSetEnd(builder)
try:
    from typing import List
except:
    pass

class SysCfgCcsSetT(object):

    # SysCfgCcsSetT
    def __init__(self):
        self.setName = None  # type: str
        self.offsetXyz = None  # type: List[float]
        self.orientation = None  # type: List[float]
        self.offsetAux = None  # type: List[float]
        self.offsetXyzunits = None  # type: List[str]
        self.orientationUnits = None  # type: List[str]
        self.offsetAuxUnits = None  # type: List[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        sysCfgCcsSet = SysCfgCcsSet()
        sysCfgCcsSet.Init(buf, pos)
        return cls.InitFromObj(sysCfgCcsSet)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, sysCfgCcsSet):
        x = SysCfgCcsSetT()
        x._UnPack(sysCfgCcsSet)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.setName == other.setName and \
            self.offsetXyz == other.offsetXyz and \
            self.orientation == other.orientation and \
            self.offsetAux == other.offsetAux and \
            self.offsetXyzunits == other.offsetXyzunits and \
            self.orientationUnits == other.orientationUnits and \
            self.offsetAuxUnits == other.offsetAuxUnits

    # SysCfgCcsSetT
    def _UnPack(self, sysCfgCcsSet):
        if sysCfgCcsSet is None:
            return
        self.setName = sysCfgCcsSet.SetName()
        if not sysCfgCcsSet.OffsetXyzIsNone():
            if np is None:
                self.offsetXyz = []
                for i in range(sysCfgCcsSet.OffsetXyzLength()):
                    self.offsetXyz.append(sysCfgCcsSet.OffsetXyz(i))
            else:
                self.offsetXyz = sysCfgCcsSet.OffsetXyzAsNumpy()
        if not sysCfgCcsSet.OrientationIsNone():
            if np is None:
                self.orientation = []
                for i in range(sysCfgCcsSet.OrientationLength()):
                    self.orientation.append(sysCfgCcsSet.Orientation(i))
            else:
                self.orientation = sysCfgCcsSet.OrientationAsNumpy()
        if not sysCfgCcsSet.OffsetAuxIsNone():
            if np is None:
                self.offsetAux = []
                for i in range(sysCfgCcsSet.OffsetAuxLength()):
                    self.offsetAux.append(sysCfgCcsSet.OffsetAux(i))
            else:
                self.offsetAux = sysCfgCcsSet.OffsetAuxAsNumpy()
        if not sysCfgCcsSet.OffsetXyzunitsIsNone():
            self.offsetXyzunits = []
            for i in range(sysCfgCcsSet.OffsetXyzunitsLength()):
                self.offsetXyzunits.append(sysCfgCcsSet.OffsetXyzunits(i))
        if not sysCfgCcsSet.OrientationUnitsIsNone():
            self.orientationUnits = []
            for i in range(sysCfgCcsSet.OrientationUnitsLength()):
                self.orientationUnits.append(sysCfgCcsSet.OrientationUnits(i))
        if not sysCfgCcsSet.OffsetAuxUnitsIsNone():
            self.offsetAuxUnits = []
            for i in range(sysCfgCcsSet.OffsetAuxUnitsLength()):
                self.offsetAuxUnits.append(sysCfgCcsSet.OffsetAuxUnits(i))

    # SysCfgCcsSetT
    def Pack(self, builder):
        if self.setName is not None:
            setName = builder.CreateString(self.setName)
        if self.offsetXyz is not None:
            if np is not None and type(self.offsetXyz) is np.ndarray:
                offsetXyz = builder.CreateNumpyVector(self.offsetXyz)
            else:
                SysCfgCcsSetStartOffsetXyzVector(builder, len(self.offsetXyz))
                for i in reversed(range(len(self.offsetXyz))):
                    builder.PrependFloat64(self.offsetXyz[i])
                offsetXyz = builder.EndVector()
        if self.orientation is not None:
            if np is not None and type(self.orientation) is np.ndarray:
                orientation = builder.CreateNumpyVector(self.orientation)
            else:
                SysCfgCcsSetStartOrientationVector(builder, len(self.orientation))
                for i in reversed(range(len(self.orientation))):
                    builder.PrependFloat64(self.orientation[i])
                orientation = builder.EndVector()
        if self.offsetAux is not None:
            if np is not None and type(self.offsetAux) is np.ndarray:
                offsetAux = builder.CreateNumpyVector(self.offsetAux)
            else:
                SysCfgCcsSetStartOffsetAuxVector(builder, len(self.offsetAux))
                for i in reversed(range(len(self.offsetAux))):
                    builder.PrependFloat64(self.offsetAux[i])
                offsetAux = builder.EndVector()
        if self.offsetXyzunits is not None:
            offsetXyzunitslist = []
            for i in range(len(self.offsetXyzunits)):
                offsetXyzunitslist.append(builder.CreateString(self.offsetXyzunits[i]))
            SysCfgCcsSetStartOffsetXyzunitsVector(builder, len(self.offsetXyzunits))
            for i in reversed(range(len(self.offsetXyzunits))):
                builder.PrependUOffsetTRelative(offsetXyzunitslist[i])
            offsetXyzunits = builder.EndVector()
        if self.orientationUnits is not None:
            orientationUnitslist = []
            for i in range(len(self.orientationUnits)):
                orientationUnitslist.append(builder.CreateString(self.orientationUnits[i]))
            SysCfgCcsSetStartOrientationUnitsVector(builder, len(self.orientationUnits))
            for i in reversed(range(len(self.orientationUnits))):
                builder.PrependUOffsetTRelative(orientationUnitslist[i])
            orientationUnits = builder.EndVector()
        if self.offsetAuxUnits is not None:
            offsetAuxUnitslist = []
            for i in range(len(self.offsetAuxUnits)):
                offsetAuxUnitslist.append(builder.CreateString(self.offsetAuxUnits[i]))
            SysCfgCcsSetStartOffsetAuxUnitsVector(builder, len(self.offsetAuxUnits))
            for i in reversed(range(len(self.offsetAuxUnits))):
                builder.PrependUOffsetTRelative(offsetAuxUnitslist[i])
            offsetAuxUnits = builder.EndVector()
        SysCfgCcsSetStart(builder)
        if self.setName is not None:
            SysCfgCcsSetAddSetName(builder, setName)
        if self.offsetXyz is not None:
            SysCfgCcsSetAddOffsetXyz(builder, offsetXyz)
        if self.orientation is not None:
            SysCfgCcsSetAddOrientation(builder, orientation)
        if self.offsetAux is not None:
            SysCfgCcsSetAddOffsetAux(builder, offsetAux)
        if self.offsetXyzunits is not None:
            SysCfgCcsSetAddOffsetXyzunits(builder, offsetXyzunits)
        if self.orientationUnits is not None:
            SysCfgCcsSetAddOrientationUnits(builder, orientationUnits)
        if self.offsetAuxUnits is not None:
            SysCfgCcsSetAddOffsetAuxUnits(builder, offsetAuxUnits)
        sysCfgCcsSet = SysCfgCcsSetEnd(builder)
        return sysCfgCcsSet
