# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One pair with value and meaning for the internal additional circle data (used for commanding)
class KinCmd2dCircleDataPair(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KinCmd2dCircleDataPair()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKinCmd2dCircleDataPair(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # KinCmd2dCircleDataPair
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # one value of a circle data
    # KinCmd2dCircleDataPair
    def Value(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # related meaning of the value
    # possible meanings are: "MP_X", "MP_Y", "MP_Z", "RADIUS", "REV"
    # KinCmd2dCircleDataPair
    def ExtMeaning(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from motion.core.fbtypes.Ext2dCircleMeaning import Ext2dCircleMeaning
            obj = Ext2dCircleMeaning()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def KinCmd2dCircleDataPairStart(builder): builder.StartObject(2)
def Start(builder):
    return KinCmd2dCircleDataPairStart(builder)
def KinCmd2dCircleDataPairAddValue(builder, value): builder.PrependFloat64Slot(0, value, 0.0)
def AddValue(builder, value):
    return KinCmd2dCircleDataPairAddValue(builder, value)
def KinCmd2dCircleDataPairAddExtMeaning(builder, extMeaning): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(extMeaning), 0)
def AddExtMeaning(builder, extMeaning):
    return KinCmd2dCircleDataPairAddExtMeaning(builder, extMeaning)
def KinCmd2dCircleDataPairEnd(builder): return builder.EndObject()
def End(builder):
    return KinCmd2dCircleDataPairEnd(builder)
import motion.core.fbtypes.Ext2dCircleMeaning
try:
    from typing import Optional
except:
    pass

class KinCmd2dCircleDataPairT(object):

    # KinCmd2dCircleDataPairT
    def __init__(self):
        self.value = 0.0  # type: float
        self.extMeaning = None  # type: Optional[motion.core.fbtypes.Ext2dCircleMeaning.Ext2dCircleMeaningT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kinCmd2dCircleDataPair = KinCmd2dCircleDataPair()
        kinCmd2dCircleDataPair.Init(buf, pos)
        return cls.InitFromObj(kinCmd2dCircleDataPair)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kinCmd2dCircleDataPair):
        x = KinCmd2dCircleDataPairT()
        x._UnPack(kinCmd2dCircleDataPair)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.value == other.value and \
            self.extMeaning == other.extMeaning

    # KinCmd2dCircleDataPairT
    def _UnPack(self, kinCmd2dCircleDataPair):
        if kinCmd2dCircleDataPair is None:
            return
        self.value = kinCmd2dCircleDataPair.Value()
        if kinCmd2dCircleDataPair.ExtMeaning() is not None:
            self.extMeaning = motion.core.fbtypes.Ext2dCircleMeaning.Ext2dCircleMeaningT.InitFromObj(kinCmd2dCircleDataPair.ExtMeaning())

    # KinCmd2dCircleDataPairT
    def Pack(self, builder):
        if self.extMeaning is not None:
            extMeaning = self.extMeaning.Pack(builder)
        KinCmd2dCircleDataPairStart(builder)
        KinCmd2dCircleDataPairAddValue(builder, self.value)
        if self.extMeaning is not None:
            KinCmd2dCircleDataPairAddExtMeaning(builder, extMeaning)
        kinCmd2dCircleDataPair = KinCmd2dCircleDataPairEnd(builder)
        return kinCmd2dCircleDataPair
