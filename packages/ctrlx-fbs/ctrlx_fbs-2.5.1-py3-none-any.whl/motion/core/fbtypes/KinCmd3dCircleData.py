# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# parameters for the move 3D circular commands for a kinematics
class KinCmd3dCircleData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KinCmd3dCircleData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKinCmd3dCircleData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # KinCmd3dCircleData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # commanded target position with meanings
    # KinCmd3dCircleData
    def CmdKinPose(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from motion.core.fbtypes.KinCmdPosePair import KinCmdPosePair
            obj = KinCmdPosePair()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # KinCmd3dCircleData
    def CmdKinPoseLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KinCmd3dCircleData
    def CmdKinPoseIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # commanded circle data with 3d circle meanings
    # KinCmd3dCircleData
    def CmdCircleData(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from motion.core.fbtypes.KinCmd3dCircleDataPair import KinCmd3dCircleDataPair
            obj = KinCmd3dCircleDataPair()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # KinCmd3dCircleData
    def CmdCircleDataLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KinCmd3dCircleData
    def CmdCircleDataIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # coordSys for commanded target position
    # KinCmd3dCircleData
    def CoordSys(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # dynamic limits for the motion of this command
    # KinCmd3dCircleData
    def Lim(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from motion.core.fbtypes.DynamicLimits import DynamicLimits
            obj = DynamicLimits()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def KinCmd3dCircleDataStart(builder): builder.StartObject(4)
def Start(builder):
    return KinCmd3dCircleDataStart(builder)
def KinCmd3dCircleDataAddCmdKinPose(builder, cmdKinPose): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(cmdKinPose), 0)
def AddCmdKinPose(builder, cmdKinPose):
    return KinCmd3dCircleDataAddCmdKinPose(builder, cmdKinPose)
def KinCmd3dCircleDataStartCmdKinPoseVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartCmdKinPoseVector(builder, numElems):
    return KinCmd3dCircleDataStartCmdKinPoseVector(builder, numElems)
def KinCmd3dCircleDataAddCmdCircleData(builder, cmdCircleData): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(cmdCircleData), 0)
def AddCmdCircleData(builder, cmdCircleData):
    return KinCmd3dCircleDataAddCmdCircleData(builder, cmdCircleData)
def KinCmd3dCircleDataStartCmdCircleDataVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartCmdCircleDataVector(builder, numElems):
    return KinCmd3dCircleDataStartCmdCircleDataVector(builder, numElems)
def KinCmd3dCircleDataAddCoordSys(builder, coordSys): builder.PrependInt8Slot(2, coordSys, 0)
def AddCoordSys(builder, coordSys):
    return KinCmd3dCircleDataAddCoordSys(builder, coordSys)
def KinCmd3dCircleDataAddLim(builder, lim): builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(lim), 0)
def AddLim(builder, lim):
    return KinCmd3dCircleDataAddLim(builder, lim)
def KinCmd3dCircleDataEnd(builder): return builder.EndObject()
def End(builder):
    return KinCmd3dCircleDataEnd(builder)
import motion.core.fbtypes.DynamicLimits
import motion.core.fbtypes.KinCmd3dCircleDataPair
import motion.core.fbtypes.KinCmdPosePair
try:
    from typing import List, Optional
except:
    pass

class KinCmd3dCircleDataT(object):

    # KinCmd3dCircleDataT
    def __init__(self):
        self.cmdKinPose = None  # type: List[motion.core.fbtypes.KinCmdPosePair.KinCmdPosePairT]
        self.cmdCircleData = None  # type: List[motion.core.fbtypes.KinCmd3dCircleDataPair.KinCmd3dCircleDataPairT]
        self.coordSys = 0  # type: int
        self.lim = None  # type: Optional[motion.core.fbtypes.DynamicLimits.DynamicLimitsT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        kinCmd3dCircleData = KinCmd3dCircleData()
        kinCmd3dCircleData.Init(buf, pos)
        return cls.InitFromObj(kinCmd3dCircleData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, kinCmd3dCircleData):
        x = KinCmd3dCircleDataT()
        x._UnPack(kinCmd3dCircleData)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.cmdKinPose == other.cmdKinPose and \
            self.cmdCircleData == other.cmdCircleData and \
            self.coordSys == other.coordSys and \
            self.lim == other.lim

    # KinCmd3dCircleDataT
    def _UnPack(self, kinCmd3dCircleData):
        if kinCmd3dCircleData is None:
            return
        if not kinCmd3dCircleData.CmdKinPoseIsNone():
            self.cmdKinPose = []
            for i in range(kinCmd3dCircleData.CmdKinPoseLength()):
                if kinCmd3dCircleData.CmdKinPose(i) is None:
                    self.cmdKinPose.append(None)
                else:
                    kinCmdPosePair_ = motion.core.fbtypes.KinCmdPosePair.KinCmdPosePairT.InitFromObj(kinCmd3dCircleData.CmdKinPose(i))
                    self.cmdKinPose.append(kinCmdPosePair_)
        if not kinCmd3dCircleData.CmdCircleDataIsNone():
            self.cmdCircleData = []
            for i in range(kinCmd3dCircleData.CmdCircleDataLength()):
                if kinCmd3dCircleData.CmdCircleData(i) is None:
                    self.cmdCircleData.append(None)
                else:
                    kinCmd3dCircleDataPair_ = motion.core.fbtypes.KinCmd3dCircleDataPair.KinCmd3dCircleDataPairT.InitFromObj(kinCmd3dCircleData.CmdCircleData(i))
                    self.cmdCircleData.append(kinCmd3dCircleDataPair_)
        self.coordSys = kinCmd3dCircleData.CoordSys()
        if kinCmd3dCircleData.Lim() is not None:
            self.lim = motion.core.fbtypes.DynamicLimits.DynamicLimitsT.InitFromObj(kinCmd3dCircleData.Lim())

    # KinCmd3dCircleDataT
    def Pack(self, builder):
        if self.cmdKinPose is not None:
            cmdKinPoselist = []
            for i in range(len(self.cmdKinPose)):
                cmdKinPoselist.append(self.cmdKinPose[i].Pack(builder))
            KinCmd3dCircleDataStartCmdKinPoseVector(builder, len(self.cmdKinPose))
            for i in reversed(range(len(self.cmdKinPose))):
                builder.PrependUOffsetTRelative(cmdKinPoselist[i])
            cmdKinPose = builder.EndVector()
        if self.cmdCircleData is not None:
            cmdCircleDatalist = []
            for i in range(len(self.cmdCircleData)):
                cmdCircleDatalist.append(self.cmdCircleData[i].Pack(builder))
            KinCmd3dCircleDataStartCmdCircleDataVector(builder, len(self.cmdCircleData))
            for i in reversed(range(len(self.cmdCircleData))):
                builder.PrependUOffsetTRelative(cmdCircleDatalist[i])
            cmdCircleData = builder.EndVector()
        if self.lim is not None:
            lim = self.lim.Pack(builder)
        KinCmd3dCircleDataStart(builder)
        if self.cmdKinPose is not None:
            KinCmd3dCircleDataAddCmdKinPose(builder, cmdKinPose)
        if self.cmdCircleData is not None:
            KinCmd3dCircleDataAddCmdCircleData(builder, cmdCircleData)
        KinCmd3dCircleDataAddCoordSys(builder, self.coordSys)
        if self.lim is not None:
            KinCmd3dCircleDataAddLim(builder, lim)
        kinCmd3dCircleData = KinCmd3dCircleDataEnd(builder)
        return kinCmd3dCircleData
