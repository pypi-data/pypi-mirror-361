[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fygaro-webhook"
version = "1.1.1"
description = "Webhook signature verification for Fygaro"
readme = { file = "README.md, CHANGELOG.md", content-type = "text/markdown" }
authors = [{ name = "Fygaro", email = "support@fygaro.com" }]
license = { file = "LICENSE" }
requires-python = ">=3.8"
keywords = ["fygaro", "webhook", "signature"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]

[project.urls]
Homepage   = "https://fygaro.com"
Changelog  = "https://fygaro.com/placeholder"

[tool.setuptools]
packages = { find = { where = ["src"], namespaces = true } }
