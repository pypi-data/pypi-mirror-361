import "charAt.bat"
import "concat.bat"
import "length.bat"
import "replace.bat"
import "contains.bat"

declare fn charAt(s: str, idx: num) -> str

declare fn concat(s1: str, s2: str) -> str

declare fn length(s: str) -> num

declare fn contains(s: str, sub: str) -> bool

declare fn replace(s: str, old: str, _new: str) -> str