# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs2

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Configuration of waitstates before transitioning to the next operating state 'state'.
# A waitstate represents a pause that is entered before switching to the next operating state 'state'.
# A waitstate pause is characterized by the component with the largest timeout value.
# A waitstate can be cancelled before the timeout is reached by:
# - a component,
# - executing the cancel command, or
# - removing the relevant waitstate from the configuration.
class WaitstateConfiguration(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = WaitstateConfiguration()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsWaitstateConfiguration(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # WaitstateConfiguration
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # List of waitstates.
    # WaitstateConfiguration
    def Components(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from common.scheduler.fbs2.WaitstateComponent import WaitstateComponent
            obj = WaitstateComponent()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # WaitstateConfiguration
    def ComponentsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # WaitstateConfiguration
    def ComponentsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def WaitstateConfigurationStart(builder): builder.StartObject(1)
def Start(builder):
    return WaitstateConfigurationStart(builder)
def WaitstateConfigurationAddComponents(builder, components): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(components), 0)
def AddComponents(builder, components):
    return WaitstateConfigurationAddComponents(builder, components)
def WaitstateConfigurationStartComponentsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartComponentsVector(builder, numElems):
    return WaitstateConfigurationStartComponentsVector(builder, numElems)
def WaitstateConfigurationEnd(builder): return builder.EndObject()
def End(builder):
    return WaitstateConfigurationEnd(builder)
import common.scheduler.fbs2.WaitstateComponent
try:
    from typing import List
except:
    pass

class WaitstateConfigurationT(object):

    # WaitstateConfigurationT
    def __init__(self):
        self.components = None  # type: List[common.scheduler.fbs2.WaitstateComponent.WaitstateComponentT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        waitstateConfiguration = WaitstateConfiguration()
        waitstateConfiguration.Init(buf, pos)
        return cls.InitFromObj(waitstateConfiguration)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, waitstateConfiguration):
        x = WaitstateConfigurationT()
        x._UnPack(waitstateConfiguration)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.components == other.components

    # WaitstateConfigurationT
    def _UnPack(self, waitstateConfiguration):
        if waitstateConfiguration is None:
            return
        if not waitstateConfiguration.ComponentsIsNone():
            self.components = []
            for i in range(waitstateConfiguration.ComponentsLength()):
                if waitstateConfiguration.Components(i) is None:
                    self.components.append(None)
                else:
                    waitstateComponent_ = common.scheduler.fbs2.WaitstateComponent.WaitstateComponentT.InitFromObj(waitstateConfiguration.Components(i))
                    self.components.append(waitstateComponent_)

    # WaitstateConfigurationT
    def Pack(self, builder):
        if self.components is not None:
            componentslist = []
            for i in range(len(self.components)):
                componentslist.append(self.components[i].Pack(builder))
            WaitstateConfigurationStartComponentsVector(builder, len(self.components))
            for i in reversed(range(len(self.components))):
                builder.PrependUOffsetTRelative(componentslist[i])
            components = builder.EndVector()
        WaitstateConfigurationStart(builder)
        if self.components is not None:
            WaitstateConfigurationAddComponents(builder, components)
        waitstateConfiguration = WaitstateConfigurationEnd(builder)
        return waitstateConfiguration
