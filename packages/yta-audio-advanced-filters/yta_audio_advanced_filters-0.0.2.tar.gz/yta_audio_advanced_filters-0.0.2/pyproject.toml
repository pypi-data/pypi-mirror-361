[project]
name = "yta-audio-advanced-filters"
version = "0.0.2"
description = "Youtube Autonomous Advanced Audio Filters Module."
authors = [
    {name = "danialcala94",email = "danielalcalavalera@gmail.com"}
]
readme = "README.md"
requires-python = "==3.9"
dependencies = [
    "yta_validation (>=0.0.1,<1.0.0)",
    "yta_programming (>=0.0.1,<1.0.0)",
    "numpy (>=0.0.1,<9.0.0)",
    "librosa (>=0.0.1,<9.0.0)",
]

[tool.poetry]
packages = [{include = "yta_audio_advanced_filters", from = "src"}]

# Optional dependencies below
[tool.poetry.group.optional]
optional = true

[tool.poetry.group.optional.dependencies]
scipy = ">=0.0.1,<9.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
