[project]
name = "jusflpyroot"
version = "0.1.2"
description = "Useful tools for root in python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.2.1",
    "console>=0.9911",
    "numpy>=2.3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
