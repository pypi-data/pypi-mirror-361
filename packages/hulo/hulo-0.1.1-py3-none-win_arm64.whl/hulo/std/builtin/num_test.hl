fn test_num() {
    assert_eq(10.to_str(), "10")

    assert(2 > 1)

    assert(Number::$MIN < Number::$INFINITY)

    assert(Number::parse("101"), 101)

    assert(Number::$PI.trunc(), 3)
}