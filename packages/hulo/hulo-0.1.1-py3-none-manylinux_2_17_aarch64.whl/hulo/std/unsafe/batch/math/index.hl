import "max.bat"
import "min.bat"
import "pi.bat"
import "abs.bat"
import "pow.bat"

declare fn min(a: num, b: num) -> num

declare fn max(a: num, b: num) -> num

declare const pi: num

declare fn abs(x: num) -> num

declare fn pow(x: num, y: num) -> num

declare fn sqrt(x: num) -> num