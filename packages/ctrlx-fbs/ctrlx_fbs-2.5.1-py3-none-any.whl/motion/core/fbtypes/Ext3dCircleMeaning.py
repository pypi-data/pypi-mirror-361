# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Packed pair of target position meaning and attribute
class Ext3dCircleMeaning(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Ext3dCircleMeaning()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsExt3dCircleMeaning(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Ext3dCircleMeaning
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # related meaning of the value
    # possible meanings are: "IP_X", "IP_Y", "IP_Z"
    # Ext3dCircleMeaning
    def Meaning(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # attributes for the target position meaning
    # possible meanings are: "UNDEF", "INCR_VALUE", "ABS_VALUE", "SHORT_WAY", "POS_ROT_DIR", "NEG_ROT_DIR"
    # Ext3dCircleMeaning
    def AddAttributes(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def Ext3dCircleMeaningStart(builder): builder.StartObject(2)
def Start(builder):
    return Ext3dCircleMeaningStart(builder)
def Ext3dCircleMeaningAddMeaning(builder, meaning): builder.PrependInt8Slot(0, meaning, 0)
def AddMeaning(builder, meaning):
    return Ext3dCircleMeaningAddMeaning(builder, meaning)
def Ext3dCircleMeaningAddAddAttributes(builder, addAttributes): builder.PrependInt8Slot(1, addAttributes, 0)
def AddAddAttributes(builder, addAttributes):
    return Ext3dCircleMeaningAddAddAttributes(builder, addAttributes)
def Ext3dCircleMeaningEnd(builder): return builder.EndObject()
def End(builder):
    return Ext3dCircleMeaningEnd(builder)

class Ext3dCircleMeaningT(object):

    # Ext3dCircleMeaningT
    def __init__(self):
        self.meaning = 0  # type: int
        self.addAttributes = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ext3dCircleMeaning = Ext3dCircleMeaning()
        ext3dCircleMeaning.Init(buf, pos)
        return cls.InitFromObj(ext3dCircleMeaning)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ext3dCircleMeaning):
        x = Ext3dCircleMeaningT()
        x._UnPack(ext3dCircleMeaning)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.meaning == other.meaning and \
            self.addAttributes == other.addAttributes

    # Ext3dCircleMeaningT
    def _UnPack(self, ext3dCircleMeaning):
        if ext3dCircleMeaning is None:
            return
        self.meaning = ext3dCircleMeaning.Meaning()
        self.addAttributes = ext3dCircleMeaning.AddAttributes()

    # Ext3dCircleMeaningT
    def Pack(self, builder):
        Ext3dCircleMeaningStart(builder)
        Ext3dCircleMeaningAddMeaning(builder, self.meaning)
        Ext3dCircleMeaningAddAddAttributes(builder, self.addAttributes)
        ext3dCircleMeaning = Ext3dCircleMeaningEnd(builder)
        return ext3dCircleMeaning
