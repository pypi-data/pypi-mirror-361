[project]
name = "cyf09-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "CYF", email = "zjw@zjw.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
cyf09-mcp-server = "cyf09_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
