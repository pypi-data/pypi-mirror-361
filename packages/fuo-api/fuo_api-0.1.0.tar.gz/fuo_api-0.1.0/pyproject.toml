[project]
name = "fuo-api"
version = "0.1.0"
description = "FUOverflow image thread API server"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name = "Your Name", email = "you@example.com"}]
license = {text = "MIT"}

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "beautifulsoup4",
    "requests"
]

[project.scripts]
fuo-api = "fuo_api.__main__:main"
