backend:
  host: "https://u468127-bbee-6742ea39.westx.seetacloud.com"
  port: 8443
  
frontend:
  # 用于向后端注册的唯一ID。如果留空，程序将自动生成一个。
  # 推荐为每个前端设置一个有意义的名称，例如: "test-pc-01", "dev-vm-ubuntu"
  id: ""
  screenshot_interval: 3.0
  # 任务模式选择 ('automatic' 或 'predefined')
  # 仅在后端 mode 设置为 'dual' 时生效
  mode: "automatic" 