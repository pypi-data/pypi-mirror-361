# automatically generated by the FlatBuffers compiler, do not modify

# namespace: builtin

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ArrayOfByteString(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayOfByteString()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayOfByteString(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ArrayOfByteString
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ArrayOfByteString
    def Array(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from comm.opcua.builtin.ByteString import ByteString
            obj = ByteString()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ArrayOfByteString
    def ArrayLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ArrayOfByteString
    def ArrayIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ArrayOfByteStringStart(builder): builder.StartObject(1)
def Start(builder):
    return ArrayOfByteStringStart(builder)
def ArrayOfByteStringAddArray(builder, array): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(array), 0)
def AddArray(builder, array):
    return ArrayOfByteStringAddArray(builder, array)
def ArrayOfByteStringStartArrayVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartArrayVector(builder, numElems):
    return ArrayOfByteStringStartArrayVector(builder, numElems)
def ArrayOfByteStringEnd(builder): return builder.EndObject()
def End(builder):
    return ArrayOfByteStringEnd(builder)
import comm.opcua.builtin.ByteString
try:
    from typing import List
except:
    pass

class ArrayOfByteStringT(object):

    # ArrayOfByteStringT
    def __init__(self):
        self.array = None  # type: List[comm.opcua.builtin.ByteString.ByteStringT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayOfByteString = ArrayOfByteString()
        arrayOfByteString.Init(buf, pos)
        return cls.InitFromObj(arrayOfByteString)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayOfByteString):
        x = ArrayOfByteStringT()
        x._UnPack(arrayOfByteString)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.array == other.array

    # ArrayOfByteStringT
    def _UnPack(self, arrayOfByteString):
        if arrayOfByteString is None:
            return
        if not arrayOfByteString.ArrayIsNone():
            self.array = []
            for i in range(arrayOfByteString.ArrayLength()):
                if arrayOfByteString.Array(i) is None:
                    self.array.append(None)
                else:
                    byteString_ = comm.opcua.builtin.ByteString.ByteStringT.InitFromObj(arrayOfByteString.Array(i))
                    self.array.append(byteString_)

    # ArrayOfByteStringT
    def Pack(self, builder):
        if self.array is not None:
            arraylist = []
            for i in range(len(self.array)):
                arraylist.append(self.array[i].Pack(builder))
            ArrayOfByteStringStartArrayVector(builder, len(self.array))
            for i in reversed(range(len(self.array))):
                builder.PrependUOffsetTRelative(arraylist[i])
            array = builder.EndVector()
        ArrayOfByteStringStart(builder)
        if self.array is not None:
            ArrayOfByteStringAddArray(builder, array)
        arrayOfByteString = ArrayOfByteStringEnd(builder)
        return arrayOfByteString
