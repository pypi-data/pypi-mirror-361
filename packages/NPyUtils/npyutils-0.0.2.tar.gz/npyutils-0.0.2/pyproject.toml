[project]
name = "NPyUtils"
version = "0.0.2"
description = "A simple Python Utils Pkg"
authors = [
  { name="namo", email="none@none.com" }
]
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.7"
keywords = ["utility", "python"]

[build-system]
requires = ["setuptools>=80.9.0"]
build-backend = "setuptools.build_meta"
