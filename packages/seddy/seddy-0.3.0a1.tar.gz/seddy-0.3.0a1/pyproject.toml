[build-system]
build-backend = "setuptools.build_meta"
requires = [
    "setuptools >= 42",
    "setuptools-scm[toml] >= 3.4",
]

[tool.setuptools_scm]

[tool.isort]
length_sort_straight = true
profile = "black"
