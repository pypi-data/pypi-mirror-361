from setuptools import setup, find_packages

setup(
	name='LengthConversion',
	version='1.0',
 	description="Module That Lets You Convert Units Of Length In Easy Way.",
	packages=find_packages(),
	install_requires=[
	],
)