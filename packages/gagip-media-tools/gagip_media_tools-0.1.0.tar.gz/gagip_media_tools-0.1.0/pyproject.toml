[project]
name = "gagip-media-tools"
version = "0.1.0"
description = "미디어 도구 모음"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "gagip", email = "y02645@gmail.com" }]
dependencies = [
    "ffmpeg-python>=0.2.0",
    "pre-commit>=4.2.0",
]
license = { text = "MIT" }

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "ruff>=0.12.2",
]
