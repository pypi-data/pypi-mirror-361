[project]
name = "multipyline"
version = "0.1.0"
description = "A Python package for easier multiline string formatting"
readme = "README.md"
authors = [
    { name = "Mojmír Majer", email = "mojmir.majer@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
