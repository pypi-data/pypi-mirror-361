[project]
name = "cakoo"
version = "0.1.1"
description = "cakoo CLI - Cross-platform C++ dev setup tool with colorful terminal UI"
readme = "README.md"
authors = [{name = "Vishal Ahirwar"}]
license = "MIT"
dependencies = ["rich>=13.0"]

[project.scripts]
cakoo = "cakoo.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
