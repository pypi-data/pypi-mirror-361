[project]
name = "mcp-server-funcat"
version = "0.1.4"
description = "MCP Server for Funcat"
readme = "README.md"
authors = [{ name = "lukbai", email = "baibaibai_000@gmail.com" }]
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.11.0","requests"]

[project.scripts]
smart-home = "mcp_server_funcat.__main__:main"

[dependency-groups]
dev = ["ruff", "ipython"]


[build-system]
requires = ["uv_build"]
build-backend = "uv_build"