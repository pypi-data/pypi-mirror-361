[project]
name = "ad-material-mcp"
version = "0.1.4"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "刘松奇", email = "" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
ad-material-mcp = "ad_material_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
