[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pg_mcp_server_beta"
version = "1.0"
description = "이 버전은 더 이상 사용되지 않는 테스트용 패키지입니다."
authors = [{name = "Your Name", email = "your@email.com"}]
readme = "README.md"
requires-python = ">=3.7" 