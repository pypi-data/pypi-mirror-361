[project]
name = "tocson-mcp-server"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tocson", email = "aiyina88@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
tocson-mcp-server = "tocson_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
