[build-system]
requires      = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name            = "central_das_emendas_teste"
version         = "0.1.0"
description     = "Baixa e carrega em DataFrame o CSV de emendas do Google Drive"
readme  = "README.md"
authors         = [{ name = "Caio Sousa", email = "c.sousa@poli.ufrj.br" }]
license = "MIT"    
requires-python = ">=3.9"
dependencies    = [
  "pandas>=2.0",
  "gdown>=5.0"
]

[project.urls]
Homepage = "https://github.com/caiosousareis"
