# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# parameters of the axis FlexProfile command
class AxsCmdFlexProfileData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AxsCmdFlexProfileData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAxsCmdFlexProfileData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # AxsCmdFlexProfileData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # name of the master axis
    # AxsCmdFlexProfileData
    def Master(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # sync source (Setpoint)
    # AxsCmdFlexProfileData
    def SyncSource(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # dynamic sync direction (ShortestWay)
    # AxsCmdFlexProfileData
    def DynSyncDirection(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # FlexProfile parameters (master offset, slave offset, ratio numerator, ratio denominator, fine adjust, profileEntry, setSelection)
    # AxsCmdFlexProfileData
    def Parameters(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from motion.sync.fbtypes.AxsCmdFlexProfileParams import AxsCmdFlexProfileParams
            obj = AxsCmdFlexProfileParams()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # should this be a buffered command?
    # AxsCmdFlexProfileData
    def Buffered(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def AxsCmdFlexProfileDataStart(builder): builder.StartObject(5)
def Start(builder):
    return AxsCmdFlexProfileDataStart(builder)
def AxsCmdFlexProfileDataAddMaster(builder, master): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(master), 0)
def AddMaster(builder, master):
    return AxsCmdFlexProfileDataAddMaster(builder, master)
def AxsCmdFlexProfileDataAddSyncSource(builder, syncSource): builder.PrependInt8Slot(1, syncSource, 0)
def AddSyncSource(builder, syncSource):
    return AxsCmdFlexProfileDataAddSyncSource(builder, syncSource)
def AxsCmdFlexProfileDataAddDynSyncDirection(builder, dynSyncDirection): builder.PrependInt8Slot(2, dynSyncDirection, 0)
def AddDynSyncDirection(builder, dynSyncDirection):
    return AxsCmdFlexProfileDataAddDynSyncDirection(builder, dynSyncDirection)
def AxsCmdFlexProfileDataAddParameters(builder, parameters): builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(parameters), 0)
def AddParameters(builder, parameters):
    return AxsCmdFlexProfileDataAddParameters(builder, parameters)
def AxsCmdFlexProfileDataAddBuffered(builder, buffered): builder.PrependBoolSlot(4, buffered, 0)
def AddBuffered(builder, buffered):
    return AxsCmdFlexProfileDataAddBuffered(builder, buffered)
def AxsCmdFlexProfileDataEnd(builder): return builder.EndObject()
def End(builder):
    return AxsCmdFlexProfileDataEnd(builder)
import motion.sync.fbtypes.AxsCmdFlexProfileParams
try:
    from typing import Optional
except:
    pass

class AxsCmdFlexProfileDataT(object):

    # AxsCmdFlexProfileDataT
    def __init__(self):
        self.master = None  # type: str
        self.syncSource = 0  # type: int
        self.dynSyncDirection = 0  # type: int
        self.parameters = None  # type: Optional[motion.sync.fbtypes.AxsCmdFlexProfileParams.AxsCmdFlexProfileParamsT]
        self.buffered = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        axsCmdFlexProfileData = AxsCmdFlexProfileData()
        axsCmdFlexProfileData.Init(buf, pos)
        return cls.InitFromObj(axsCmdFlexProfileData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, axsCmdFlexProfileData):
        x = AxsCmdFlexProfileDataT()
        x._UnPack(axsCmdFlexProfileData)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.master == other.master and \
            self.syncSource == other.syncSource and \
            self.dynSyncDirection == other.dynSyncDirection and \
            self.parameters == other.parameters and \
            self.buffered == other.buffered

    # AxsCmdFlexProfileDataT
    def _UnPack(self, axsCmdFlexProfileData):
        if axsCmdFlexProfileData is None:
            return
        self.master = axsCmdFlexProfileData.Master()
        self.syncSource = axsCmdFlexProfileData.SyncSource()
        self.dynSyncDirection = axsCmdFlexProfileData.DynSyncDirection()
        if axsCmdFlexProfileData.Parameters() is not None:
            self.parameters = motion.sync.fbtypes.AxsCmdFlexProfileParams.AxsCmdFlexProfileParamsT.InitFromObj(axsCmdFlexProfileData.Parameters())
        self.buffered = axsCmdFlexProfileData.Buffered()

    # AxsCmdFlexProfileDataT
    def Pack(self, builder):
        if self.master is not None:
            master = builder.CreateString(self.master)
        if self.parameters is not None:
            parameters = self.parameters.Pack(builder)
        AxsCmdFlexProfileDataStart(builder)
        if self.master is not None:
            AxsCmdFlexProfileDataAddMaster(builder, master)
        AxsCmdFlexProfileDataAddSyncSource(builder, self.syncSource)
        AxsCmdFlexProfileDataAddDynSyncDirection(builder, self.dynSyncDirection)
        if self.parameters is not None:
            AxsCmdFlexProfileDataAddParameters(builder, parameters)
        AxsCmdFlexProfileDataAddBuffered(builder, self.buffered)
        axsCmdFlexProfileData = AxsCmdFlexProfileDataEnd(builder)
        return axsCmdFlexProfileData
