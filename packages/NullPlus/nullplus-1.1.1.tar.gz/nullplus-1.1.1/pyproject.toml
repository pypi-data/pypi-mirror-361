[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "NullPlus"
version = "1.1.1"
description = "A simple class that allows for error handling and eating inputs"
readme = "README.md"
authors = [{ name = "StrawberryMilcRivers", email = "chocomilccy@gmail.com" }]
license = "MIT"
license-files = ["LICENSE*"]
requires-python = ">=3.8"
dependencies = []
