[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["masked_norm"]

[project]
name = "masked_norm"

authors = [
    {name = "algmarques"}
]
maintainers = [
    {name = "algmarques"}
]

license = "MIT"
license-files = ["LICENSE"]

version = "2025.7"

requires-python = ">= 3.12"
dependencies = [
    "torch >= 2.7.1",
    "numpy >= 2.3.1"
]

readme = "README.md"

[project.urls]
Repository = "https://github.com/algmarques/masked_norm.git"
