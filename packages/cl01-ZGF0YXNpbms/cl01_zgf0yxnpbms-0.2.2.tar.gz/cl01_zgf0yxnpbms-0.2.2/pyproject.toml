[tool.poetry]
name = "cl01-ZGF0YXNpbms"
version = "0.2.2"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
license = "LICENSE"
packages = [{include = "data_sink_api_client"}]

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
requests = "^2.31.0"
pydantic = "^2.7.1"
httpx = "^0.27.0"

[project]
name = "cl01-ZGF0YXNpbms"
version = "0.2.2"
description = ""
readme = ""
requires-python = ">=3.10,<4.0"
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]
dependencies = [
  "requests",
  "pydantic",
  "httpx",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
