[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "turtlesim_richtext"
version = "0.0.1"
description = "A Python library for editing richtext in TurtleSim."
license = {text = "MIT"}
authors = [{name = "oncepython"}]
dependencies = []
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]
