# setup.py

from setuptools import setup, find_packages

# This file is included for compatibility with older tooling.
# All project metadata is now defined in pyproject.toml.
setup(
    packages=find_packages(),
)