[project]
name = "mad-services"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "glaunay", email = "guillaume.launay@ens-lyon.fr" }
]
requires-python = ">=3.11"
dependencies = [
    "mdanalysis>=2.9.0",
    "numpy>=2.3.1",
]

[project.scripts]
mad-boxer = "mad_services.box:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
