[project]
name = 'ppecli'
version = '0.1.0'
description = 'Python Ping with Emoji'
authors = [
    { name = "Froas", email = 'froas.dev@gmail.com' }
]

readme = 'README.md'
license = {file = "LICENCE"}
dependencies = ['requests']

[project.scripts]
cping = 'main:main'