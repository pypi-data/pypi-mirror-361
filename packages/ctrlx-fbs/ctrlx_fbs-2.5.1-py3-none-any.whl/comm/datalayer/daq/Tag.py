# automatically generated by the FlatBuffers compiler, do not modify

# namespace: daq

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# tag as key-value-pair for meta data
class Tag(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Tag()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTag(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Tag
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # unique key of the tag
    # Tag
    def Key(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # value of the tag
    # Tag
    def Value(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TagStart(builder): builder.StartObject(2)
def Start(builder):
    return TagStart(builder)
def TagAddKey(builder, key): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(key), 0)
def AddKey(builder, key):
    return TagAddKey(builder, key)
def TagAddValue(builder, value): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(value), 0)
def AddValue(builder, value):
    return TagAddValue(builder, value)
def TagEnd(builder): return builder.EndObject()
def End(builder):
    return TagEnd(builder)

class TagT(object):

    # TagT
    def __init__(self):
        self.key = None  # type: str
        self.value = None  # type: str

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tag = Tag()
        tag.Init(buf, pos)
        return cls.InitFromObj(tag)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tag):
        x = TagT()
        x._UnPack(tag)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.key == other.key and \
            self.value == other.value

    # TagT
    def _UnPack(self, tag):
        if tag is None:
            return
        self.key = tag.Key()
        self.value = tag.Value()

    # TagT
    def Pack(self, builder):
        if self.key is not None:
            key = builder.CreateString(self.key)
        if self.value is not None:
            value = builder.CreateString(self.value)
        TagStart(builder)
        if self.key is not None:
            TagAddKey(builder, key)
        if self.value is not None:
            TagAddValue(builder, value)
        tag = TagEnd(builder)
        return tag
