[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "contact-email-address-local"
version = "0.0.25" # https://pypi.org/project/contact-email-address-local
description = "contact-email-address-local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]
