[project]
name = "add-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ashutoshsahu-13", email = "ashutoshsahu243@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "build>=1.2.2.post1",
    "mcp[cli]>=1.11.0",
    "twine>=6.1.0",
]

[project.scripts]
add-server = "add_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
