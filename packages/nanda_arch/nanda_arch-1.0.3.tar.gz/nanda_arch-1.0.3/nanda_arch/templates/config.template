from apps.{{ name }}.router import router
from nanda_arch.core.base import AppConfigBase


class AppConfig(AppConfigBase):
    name = '{{ name }}'
    router = router
    models = ['apps.{{ name }}.models']
    db_connection = 'default'