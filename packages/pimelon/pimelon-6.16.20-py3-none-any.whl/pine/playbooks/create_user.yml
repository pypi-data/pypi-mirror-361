---

  - hosts: localhost
    become: yes
    become_user: root
    tasks:
    - name: Create user
      user:
        name: '{{ melon_user }}'
        generate_ssh_key: yes

    - name: Set home folder perms
      file:
        path: '{{ user_directory }}'
        mode: 'o+rx'
        owner: '{{ melon_user }}'
        group: '{{ melon_user }}'
        recurse: yes

    - name: Set /tmp/.pine folder perms
      file:
        path: '{{ repo_path }}'
        owner: '{{ melon_user }}'
        group: '{{ melon_user }}'
        recurse: yes

    - name: Change default shell to bash
      shell: "chsh {{ melon_user }} -s $(which bash)"
...
