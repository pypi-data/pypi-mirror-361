[project]
name = "customcircle"
version = "0.1.0"
description = "Generate a circle image with a user's name inside"
authors = [{ name="Prateek", email="prateekact17@gmail.com" }]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
dependencies = [
    "Pillow"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
