[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hello_web"
version = "0.1.1"
description = "A flask demo return hello"
authors = [{name = "Your Name", email = "your.email@example.com"}]
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.1",
]

[project.scripts]
hello-web = "hello_web.main:main"
