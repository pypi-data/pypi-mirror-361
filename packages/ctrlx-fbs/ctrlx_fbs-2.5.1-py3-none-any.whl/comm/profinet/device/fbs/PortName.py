# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PortName(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PortName()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPortName(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PortName
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PortName
    def Request(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.profinet.device.fbs.PortNameRequest import PortNameRequest
            obj = PortNameRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PortName
    def Response(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.profinet.device.fbs.PortNameResponse import PortNameResponse
            obj = PortNameResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PortNameStart(builder): builder.StartObject(2)
def Start(builder):
    return PortNameStart(builder)
def PortNameAddRequest(builder, request): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(request), 0)
def AddRequest(builder, request):
    return PortNameAddRequest(builder, request)
def PortNameAddResponse(builder, response): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(response), 0)
def AddResponse(builder, response):
    return PortNameAddResponse(builder, response)
def PortNameEnd(builder): return builder.EndObject()
def End(builder):
    return PortNameEnd(builder)
import comm.profinet.device.fbs.PortNameRequest
import comm.profinet.device.fbs.PortNameResponse
try:
    from typing import Optional
except:
    pass

class PortNameT(object):

    # PortNameT
    def __init__(self):
        self.request = None  # type: Optional[comm.profinet.device.fbs.PortNameRequest.PortNameRequestT]
        self.response = None  # type: Optional[comm.profinet.device.fbs.PortNameResponse.PortNameResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        portName = PortName()
        portName.Init(buf, pos)
        return cls.InitFromObj(portName)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, portName):
        x = PortNameT()
        x._UnPack(portName)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.request == other.request and \
            self.response == other.response

    # PortNameT
    def _UnPack(self, portName):
        if portName is None:
            return
        if portName.Request() is not None:
            self.request = comm.profinet.device.fbs.PortNameRequest.PortNameRequestT.InitFromObj(portName.Request())
        if portName.Response() is not None:
            self.response = comm.profinet.device.fbs.PortNameResponse.PortNameResponseT.InitFromObj(portName.Response())

    # PortNameT
    def Pack(self, builder):
        if self.request is not None:
            request = self.request.Pack(builder)
        if self.response is not None:
            response = self.response.Pack(builder)
        PortNameStart(builder)
        if self.request is not None:
            PortNameAddRequest(builder, request)
        if self.response is not None:
            PortNameAddResponse(builder, response)
        portName = PortNameEnd(builder)
        return portName
