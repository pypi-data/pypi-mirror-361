[project]
name = "mc-postgres-db"
version = "1.0.10"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.16.2",
    "psycopg2-binary>=2.9.10",
    "ruff>=0.12.0",
    "sqlalchemy>=2.0.41",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.1.0",
]
test = [
    "pytest>=8.4.1",
]
