# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

#Thsi node converts master sync unit id to name.
class MsuIdToName(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MsuIdToName()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMsuIdToName(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # MsuIdToName
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MsuIdToName
    def Request(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.ethercat.master.fbs.MsuIdToNameRequest import MsuIdToNameRequest
            obj = MsuIdToNameRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MsuIdToName
    def Response(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.ethercat.master.fbs.MsuIdToNameResponse import MsuIdToNameResponse
            obj = MsuIdToNameResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def MsuIdToNameStart(builder): builder.StartObject(2)
def Start(builder):
    return MsuIdToNameStart(builder)
def MsuIdToNameAddRequest(builder, request): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(request), 0)
def AddRequest(builder, request):
    return MsuIdToNameAddRequest(builder, request)
def MsuIdToNameAddResponse(builder, response): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(response), 0)
def AddResponse(builder, response):
    return MsuIdToNameAddResponse(builder, response)
def MsuIdToNameEnd(builder): return builder.EndObject()
def End(builder):
    return MsuIdToNameEnd(builder)
import comm.ethercat.master.fbs.MsuIdToNameRequest
import comm.ethercat.master.fbs.MsuIdToNameResponse
try:
    from typing import Optional
except:
    pass

class MsuIdToNameT(object):

    # MsuIdToNameT
    def __init__(self):
        self.request = None  # type: Optional[comm.ethercat.master.fbs.MsuIdToNameRequest.MsuIdToNameRequestT]
        self.response = None  # type: Optional[comm.ethercat.master.fbs.MsuIdToNameResponse.MsuIdToNameResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        msuIdToName = MsuIdToName()
        msuIdToName.Init(buf, pos)
        return cls.InitFromObj(msuIdToName)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, msuIdToName):
        x = MsuIdToNameT()
        x._UnPack(msuIdToName)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.request == other.request and \
            self.response == other.response

    # MsuIdToNameT
    def _UnPack(self, msuIdToName):
        if msuIdToName is None:
            return
        if msuIdToName.Request() is not None:
            self.request = comm.ethercat.master.fbs.MsuIdToNameRequest.MsuIdToNameRequestT.InitFromObj(msuIdToName.Request())
        if msuIdToName.Response() is not None:
            self.response = comm.ethercat.master.fbs.MsuIdToNameResponse.MsuIdToNameResponseT.InitFromObj(msuIdToName.Response())

    # MsuIdToNameT
    def Pack(self, builder):
        if self.request is not None:
            request = self.request.Pack(builder)
        if self.response is not None:
            response = self.response.Pack(builder)
        MsuIdToNameStart(builder)
        if self.request is not None:
            MsuIdToNameAddRequest(builder, request)
        if self.response is not None:
            MsuIdToNameAddResponse(builder, response)
        msuIdToName = MsuIdToNameEnd(builder)
        return msuIdToName
