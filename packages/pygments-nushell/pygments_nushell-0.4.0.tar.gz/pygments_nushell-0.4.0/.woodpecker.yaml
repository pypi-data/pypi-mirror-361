steps:
  pypi-release:
    image: ghcr.io/astral-sh/uv:debian
    pull: true
    when:
      event: tag
    commands:
      - ./ci/publish.sh
    environment:
      pypi_token:
        from_secret: pypi_token
