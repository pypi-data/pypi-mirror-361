# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class NetXs(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = NetXs()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsNetXs(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # NetXs
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # NetXs
    def Values(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from comm.profinet.device.fbs.NetX import NetX
            obj = NetX()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # NetXs
    def ValuesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # NetXs
    def ValuesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def NetXsStart(builder): builder.StartObject(1)
def Start(builder):
    return NetXsStart(builder)
def NetXsAddValues(builder, values): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(values), 0)
def AddValues(builder, values):
    return NetXsAddValues(builder, values)
def NetXsStartValuesVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartValuesVector(builder, numElems):
    return NetXsStartValuesVector(builder, numElems)
def NetXsEnd(builder): return builder.EndObject()
def End(builder):
    return NetXsEnd(builder)
import comm.profinet.device.fbs.NetX
try:
    from typing import List
except:
    pass

class NetXsT(object):

    # NetXsT
    def __init__(self):
        self.values = None  # type: List[comm.profinet.device.fbs.NetX.NetXT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        netXs = NetXs()
        netXs.Init(buf, pos)
        return cls.InitFromObj(netXs)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, netXs):
        x = NetXsT()
        x._UnPack(netXs)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.values == other.values

    # NetXsT
    def _UnPack(self, netXs):
        if netXs is None:
            return
        if not netXs.ValuesIsNone():
            self.values = []
            for i in range(netXs.ValuesLength()):
                if netXs.Values(i) is None:
                    self.values.append(None)
                else:
                    netX_ = comm.profinet.device.fbs.NetX.NetXT.InitFromObj(netXs.Values(i))
                    self.values.append(netX_)

    # NetXsT
    def Pack(self, builder):
        if self.values is not None:
            valueslist = []
            for i in range(len(self.values)):
                valueslist.append(self.values[i].Pack(builder))
            NetXsStartValuesVector(builder, len(self.values))
            for i in reversed(range(len(self.values))):
                builder.PrependUOffsetTRelative(valueslist[i])
            values = builder.EndVector()
        NetXsStart(builder)
        if self.values is not None:
            NetXsAddValues(builder, values)
        netXs = NetXsEnd(builder)
        return netXs
