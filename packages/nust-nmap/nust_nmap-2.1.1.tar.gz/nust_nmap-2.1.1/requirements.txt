# No runtime dependencies - nust-nmap uses only standard library
# optional psutil dependency for performance monitoring
psutil>=5.6.7
# Development dependencies (install with: pip install -r requirements-dev.txt)
