[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "etiennevar"
version = "1.0.4"
description = "Outil en ligne de commande pour résumer et visualiser les fichiers VCF"
authors = [
    { name="Etienne Kabongo", email="etienne.ntumba.kabongo@umontreal.ca" }
]
readme = "README.md"
requires-python = ">=3.7"
keywords = ["vcf", "bioinformatique", "variant", "cli", "visualisation"]
license = "MIT"

dependencies = [
    "matplotlib",
    "numpy",
    "seaborn" 
]

[project.urls]
Homepage = "https://pypi.org/project/etiennevar"
Repository = "https://github.com/etiennekabongo/etiennevar"

[project.scripts]
etiennevar = "etiennevar.cli:main"

