[project]
name = "usman-ghani"
version = "0.0.1"
description = "Test upload to verify ownership"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
