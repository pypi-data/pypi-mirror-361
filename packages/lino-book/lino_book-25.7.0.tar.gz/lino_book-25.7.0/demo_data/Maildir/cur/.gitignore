# The Mailbox format requires three subdirs "tmp", "new" and "cur". Our example
# needs only the directory "new", the other two are empty.  We instruct Git to
# ignore everything in this directory except this .gitignore file:

*
!.gitignore
