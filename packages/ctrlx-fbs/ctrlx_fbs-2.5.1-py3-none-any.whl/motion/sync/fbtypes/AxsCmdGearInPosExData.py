# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# parameters of the axis GearInPosEx command
class AxsCmdGearInPosExData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AxsCmdGearInPosExData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAxsCmdGearInPosExData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # AxsCmdGearInPosExData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # name of the master axis
    # AxsCmdGearInPosExData
    def Master(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # sync source (Actual/Setpoint/Actual-extrapolated)
    # AxsCmdGearInPosExData
    def SyncSource(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # sync mode (Abs/Rel; Init/KeepState)
    # AxsCmdGearInPosExData
    def SyncMode(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # dynamic sync direction (Positive/Negative/ShortestWay)
    # AxsCmdGearInPosExData
    def DynSyncDirection(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # gear in pos parameters (master offset, slave offset, master ratio, slave ratio, fine adjust)
    # AxsCmdGearInPosExData
    def Parameters(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from motion.sync.fbtypes.AxsCmdGearInPosParams import AxsCmdGearInPosParams
            obj = AxsCmdGearInPosParams()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # gear in pos data layer parameters
    # AxsCmdGearInPosExData
    def DlParameters(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from motion.sync.fbtypes.AxsCmdGearInPosDlParams import AxsCmdGearInPosDlParams
            obj = AxsCmdGearInPosDlParams()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # should this be a buffered command?
    # AxsCmdGearInPosExData
    def Buffered(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def AxsCmdGearInPosExDataStart(builder): builder.StartObject(7)
def Start(builder):
    return AxsCmdGearInPosExDataStart(builder)
def AxsCmdGearInPosExDataAddMaster(builder, master): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(master), 0)
def AddMaster(builder, master):
    return AxsCmdGearInPosExDataAddMaster(builder, master)
def AxsCmdGearInPosExDataAddSyncSource(builder, syncSource): builder.PrependInt8Slot(1, syncSource, 0)
def AddSyncSource(builder, syncSource):
    return AxsCmdGearInPosExDataAddSyncSource(builder, syncSource)
def AxsCmdGearInPosExDataAddSyncMode(builder, syncMode): builder.PrependInt8Slot(2, syncMode, 0)
def AddSyncMode(builder, syncMode):
    return AxsCmdGearInPosExDataAddSyncMode(builder, syncMode)
def AxsCmdGearInPosExDataAddDynSyncDirection(builder, dynSyncDirection): builder.PrependInt8Slot(3, dynSyncDirection, 0)
def AddDynSyncDirection(builder, dynSyncDirection):
    return AxsCmdGearInPosExDataAddDynSyncDirection(builder, dynSyncDirection)
def AxsCmdGearInPosExDataAddParameters(builder, parameters): builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(parameters), 0)
def AddParameters(builder, parameters):
    return AxsCmdGearInPosExDataAddParameters(builder, parameters)
def AxsCmdGearInPosExDataAddDlParameters(builder, dlParameters): builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(dlParameters), 0)
def AddDlParameters(builder, dlParameters):
    return AxsCmdGearInPosExDataAddDlParameters(builder, dlParameters)
def AxsCmdGearInPosExDataAddBuffered(builder, buffered): builder.PrependBoolSlot(6, buffered, 0)
def AddBuffered(builder, buffered):
    return AxsCmdGearInPosExDataAddBuffered(builder, buffered)
def AxsCmdGearInPosExDataEnd(builder): return builder.EndObject()
def End(builder):
    return AxsCmdGearInPosExDataEnd(builder)
import motion.sync.fbtypes.AxsCmdGearInPosDlParams
import motion.sync.fbtypes.AxsCmdGearInPosParams
try:
    from typing import Optional
except:
    pass

class AxsCmdGearInPosExDataT(object):

    # AxsCmdGearInPosExDataT
    def __init__(self):
        self.master = None  # type: str
        self.syncSource = 0  # type: int
        self.syncMode = 0  # type: int
        self.dynSyncDirection = 0  # type: int
        self.parameters = None  # type: Optional[motion.sync.fbtypes.AxsCmdGearInPosParams.AxsCmdGearInPosParamsT]
        self.dlParameters = None  # type: Optional[motion.sync.fbtypes.AxsCmdGearInPosDlParams.AxsCmdGearInPosDlParamsT]
        self.buffered = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        axsCmdGearInPosExData = AxsCmdGearInPosExData()
        axsCmdGearInPosExData.Init(buf, pos)
        return cls.InitFromObj(axsCmdGearInPosExData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, axsCmdGearInPosExData):
        x = AxsCmdGearInPosExDataT()
        x._UnPack(axsCmdGearInPosExData)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.master == other.master and \
            self.syncSource == other.syncSource and \
            self.syncMode == other.syncMode and \
            self.dynSyncDirection == other.dynSyncDirection and \
            self.parameters == other.parameters and \
            self.dlParameters == other.dlParameters and \
            self.buffered == other.buffered

    # AxsCmdGearInPosExDataT
    def _UnPack(self, axsCmdGearInPosExData):
        if axsCmdGearInPosExData is None:
            return
        self.master = axsCmdGearInPosExData.Master()
        self.syncSource = axsCmdGearInPosExData.SyncSource()
        self.syncMode = axsCmdGearInPosExData.SyncMode()
        self.dynSyncDirection = axsCmdGearInPosExData.DynSyncDirection()
        if axsCmdGearInPosExData.Parameters() is not None:
            self.parameters = motion.sync.fbtypes.AxsCmdGearInPosParams.AxsCmdGearInPosParamsT.InitFromObj(axsCmdGearInPosExData.Parameters())
        if axsCmdGearInPosExData.DlParameters() is not None:
            self.dlParameters = motion.sync.fbtypes.AxsCmdGearInPosDlParams.AxsCmdGearInPosDlParamsT.InitFromObj(axsCmdGearInPosExData.DlParameters())
        self.buffered = axsCmdGearInPosExData.Buffered()

    # AxsCmdGearInPosExDataT
    def Pack(self, builder):
        if self.master is not None:
            master = builder.CreateString(self.master)
        if self.parameters is not None:
            parameters = self.parameters.Pack(builder)
        if self.dlParameters is not None:
            dlParameters = self.dlParameters.Pack(builder)
        AxsCmdGearInPosExDataStart(builder)
        if self.master is not None:
            AxsCmdGearInPosExDataAddMaster(builder, master)
        AxsCmdGearInPosExDataAddSyncSource(builder, self.syncSource)
        AxsCmdGearInPosExDataAddSyncMode(builder, self.syncMode)
        AxsCmdGearInPosExDataAddDynSyncDirection(builder, self.dynSyncDirection)
        if self.parameters is not None:
            AxsCmdGearInPosExDataAddParameters(builder, parameters)
        if self.dlParameters is not None:
            AxsCmdGearInPosExDataAddDlParameters(builder, dlParameters)
        AxsCmdGearInPosExDataAddBuffered(builder, self.buffered)
        axsCmdGearInPosExData = AxsCmdGearInPosExDataEnd(builder)
        return axsCmdGearInPosExData
