[project]
name = "keybind-vault"
version = "1.0.4"
description = "Text User Interface (TUI) that allows users to manage keybindings"
readme = "README.md"
authors = [
  { name = "Aaron Muñoz", email = "Thompson6626@gmail.com" }
]
requires-python = ">=3.13"
license = "MIT"
dependencies = [
  "sqlite-utils>=3.38",
  "textual>=3.4.0",
  "textual-dev>=1.7.0",
]
# Note: hatchling itself is a build dependency, not a runtime dep.

[project.scripts]
keybind-vault = "keybind_vault.main:main"

[project.optional-dependencies]
dev = ["ruff>=0.11.13"]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project.urls]
Issues = "https://github.com/Thompson6626/Keybind-Vault/issues"
Source = "https://github.com/Thompson6626/Keybind-Vault"