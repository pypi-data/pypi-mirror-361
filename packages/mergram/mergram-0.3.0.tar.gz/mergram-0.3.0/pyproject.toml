[project]
name = "mergram"
version = "0.3.0"
description = "Programmatically construct Mermaid diagrams and charts in Python"
readme = "README.md"
authors = [
    { name = "Blake A. Wilson", email = "blakeaw1102@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]

[project.optional-dependencies]
networkx = ["networkx"]

[project.urls]
Repository = "https://github.com/blakeaw/mergram"
Issues = "https://github.com/blakeaw/mergram/issues"
Changelog = "https://github.com/blakeaw/mergram/blob/main/CHANGELOG.md"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"