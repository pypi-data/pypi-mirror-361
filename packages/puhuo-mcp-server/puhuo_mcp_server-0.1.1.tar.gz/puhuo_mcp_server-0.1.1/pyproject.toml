[project]
name = "puhuo-mcp-server"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wuzhang.wz", email = "wuzhang.wz@alibaba-inc.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
puhuo-mcp-server = "puhuo_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
