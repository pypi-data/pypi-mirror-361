# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbs

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class IpSettings(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = IpSettings()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsIpSettings(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # IpSettings
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # IpSettings
    def Request(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.profinet.device.fbs.IpSettingsRequest import IpSettingsRequest
            obj = IpSettingsRequest()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # IpSettings
    def Response(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.profinet.device.fbs.IpSettingsResponse import IpSettingsResponse
            obj = IpSettingsResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def IpSettingsStart(builder): builder.StartObject(2)
def Start(builder):
    return IpSettingsStart(builder)
def IpSettingsAddRequest(builder, request): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(request), 0)
def AddRequest(builder, request):
    return IpSettingsAddRequest(builder, request)
def IpSettingsAddResponse(builder, response): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(response), 0)
def AddResponse(builder, response):
    return IpSettingsAddResponse(builder, response)
def IpSettingsEnd(builder): return builder.EndObject()
def End(builder):
    return IpSettingsEnd(builder)
import comm.profinet.device.fbs.IpSettingsRequest
import comm.profinet.device.fbs.IpSettingsResponse
try:
    from typing import Optional
except:
    pass

class IpSettingsT(object):

    # IpSettingsT
    def __init__(self):
        self.request = None  # type: Optional[comm.profinet.device.fbs.IpSettingsRequest.IpSettingsRequestT]
        self.response = None  # type: Optional[comm.profinet.device.fbs.IpSettingsResponse.IpSettingsResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ipSettings = IpSettings()
        ipSettings.Init(buf, pos)
        return cls.InitFromObj(ipSettings)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ipSettings):
        x = IpSettingsT()
        x._UnPack(ipSettings)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.request == other.request and \
            self.response == other.response

    # IpSettingsT
    def _UnPack(self, ipSettings):
        if ipSettings is None:
            return
        if ipSettings.Request() is not None:
            self.request = comm.profinet.device.fbs.IpSettingsRequest.IpSettingsRequestT.InitFromObj(ipSettings.Request())
        if ipSettings.Response() is not None:
            self.response = comm.profinet.device.fbs.IpSettingsResponse.IpSettingsResponseT.InitFromObj(ipSettings.Response())

    # IpSettingsT
    def Pack(self, builder):
        if self.request is not None:
            request = self.request.Pack(builder)
        if self.response is not None:
            response = self.response.Pack(builder)
        IpSettingsStart(builder)
        if self.request is not None:
            IpSettingsAddRequest(builder, request)
        if self.response is not None:
            IpSettingsAddResponse(builder, response)
        ipSettings = IpSettingsEnd(builder)
        return ipSettings
