PLACEHOLDER_TYPE:
  count_rows:
    metric_query: COUNT(*)
    metric_return_datatype: INTEGER
  count_null:
    metric_query: SUM(CASE WHEN "{{ col_name }}" IS NULL THEN 1 ELSE 0 END)
    metric_return_datatype: INTEGER
