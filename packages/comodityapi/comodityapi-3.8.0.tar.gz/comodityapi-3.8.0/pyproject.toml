[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "comodityapi"
version = "3.8.0"
description = "Python SDK for commodity price data via API Freaks"
readme = "README.md"
requires-python = ">=3.7"
authors = [
    { name="Muhammad Moeer", email="moeer3505@gmail.com" }
]
license = {text = "MIT"}
dependencies = ["requests>=2.20.0"]