[project]
name = "mpsapi"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dmitry Drozdov", email = "ds.drozdov@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
mpsapi = "mpsapi:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
