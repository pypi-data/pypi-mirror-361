[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fivemisrael"
version = "1.0.1"
description = "Easy vote handling for FiveM Israel in Python"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    { name = "Ticker" }
]
dependencies = [
    "websockets>=10.0"
]
