[project]
name = "cyclonepackagedemo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
cyclonepackagedemo = "cyclonepackagedemo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
