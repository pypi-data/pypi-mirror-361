# automatically generated by the FlatBuffers compiler, do not modify

# namespace: builtin

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ArrayOfStatusCode(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayOfStatusCode()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayOfStatusCode(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ArrayOfStatusCode
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ArrayOfStatusCode
    def Array(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from comm.opcua.builtin.StatusCode import StatusCode
            obj = StatusCode()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ArrayOfStatusCode
    def ArrayLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ArrayOfStatusCode
    def ArrayIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ArrayOfStatusCodeStart(builder): builder.StartObject(1)
def Start(builder):
    return ArrayOfStatusCodeStart(builder)
def ArrayOfStatusCodeAddArray(builder, array): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(array), 0)
def AddArray(builder, array):
    return ArrayOfStatusCodeAddArray(builder, array)
def ArrayOfStatusCodeStartArrayVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartArrayVector(builder, numElems):
    return ArrayOfStatusCodeStartArrayVector(builder, numElems)
def ArrayOfStatusCodeEnd(builder): return builder.EndObject()
def End(builder):
    return ArrayOfStatusCodeEnd(builder)
import comm.opcua.builtin.StatusCode
try:
    from typing import List
except:
    pass

class ArrayOfStatusCodeT(object):

    # ArrayOfStatusCodeT
    def __init__(self):
        self.array = None  # type: List[comm.opcua.builtin.StatusCode.StatusCodeT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayOfStatusCode = ArrayOfStatusCode()
        arrayOfStatusCode.Init(buf, pos)
        return cls.InitFromObj(arrayOfStatusCode)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayOfStatusCode):
        x = ArrayOfStatusCodeT()
        x._UnPack(arrayOfStatusCode)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.array == other.array

    # ArrayOfStatusCodeT
    def _UnPack(self, arrayOfStatusCode):
        if arrayOfStatusCode is None:
            return
        if not arrayOfStatusCode.ArrayIsNone():
            self.array = []
            for i in range(arrayOfStatusCode.ArrayLength()):
                if arrayOfStatusCode.Array(i) is None:
                    self.array.append(None)
                else:
                    statusCode_ = comm.opcua.builtin.StatusCode.StatusCodeT.InitFromObj(arrayOfStatusCode.Array(i))
                    self.array.append(statusCode_)

    # ArrayOfStatusCodeT
    def Pack(self, builder):
        if self.array is not None:
            arraylist = []
            for i in range(len(self.array)):
                arraylist.append(self.array[i].Pack(builder))
            ArrayOfStatusCodeStartArrayVector(builder, len(self.array))
            for i in reversed(range(len(self.array))):
                builder.PrependUOffsetTRelative(arraylist[i])
            array = builder.EndVector()
        ArrayOfStatusCodeStart(builder)
        if self.array is not None:
            ArrayOfStatusCodeAddArray(builder, array)
        arrayOfStatusCode = ArrayOfStatusCodeEnd(builder)
        return arrayOfStatusCode
