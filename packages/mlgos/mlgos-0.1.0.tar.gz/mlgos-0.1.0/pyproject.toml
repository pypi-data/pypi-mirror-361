[build-system]
requires = ["scikit-build-core>=0.8", "pybind11", "cmake"]
build-backend = "scikit_build_core.build"

[project]
name = "mlgos"
version = "0.1.0"
description = "A C++ linear regression extension using pybind11"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Your Name", email = "your@email.com" }
]

[tool.scikit-build]
wheel.expand-macos-universal-tags = true
cmake.version = ">=3.15"