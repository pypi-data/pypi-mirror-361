[project]
name = "mindtrace-datalake"
version = "0.1.0"
description = "Datalake functionality for Mindtrace"
license = "Apache-2.0"
authors = [
    {name = "Mindtrace Team"}
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "mindtrace-core",
    "mindtrace-registry",
    "mindtrace-database",
    "mindtrace-services",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
