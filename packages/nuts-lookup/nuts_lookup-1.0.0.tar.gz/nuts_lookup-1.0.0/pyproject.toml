[project]
name = "nuts-lookup"
version = "1.0.0"
description = "Lookup NUTS codes from country and ZIP code"
readme = "README.md"
authors = [
    { name = "Fabian Köll", email = "koell.dev@google.com" }
]
license = "MIT"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
nuts-lookup = "nuts_lookup.cli:main"

[tool.setuptools.package-data]
"nuts_lookup" = ["data/nuts.db"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
