[project]
name = "iamusmanbro"
version = "0.0.1"
description = "Alias for usman-ghani"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "usman-ghani"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
