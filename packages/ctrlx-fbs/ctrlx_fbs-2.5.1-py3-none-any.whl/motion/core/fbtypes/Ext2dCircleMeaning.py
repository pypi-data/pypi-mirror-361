# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Packed pair of target position meaning and attribute
class Ext2dCircleMeaning(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Ext2dCircleMeaning()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsExt2dCircleMeaning(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Ext2dCircleMeaning
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # related meaning of the value
    # possible meanings are: "RADIUS", "CP_X", "CP_Y", "CP_Z", "REV"
    # Ext2dCircleMeaning
    def Meaning(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # attributes for the target position meaning
    # possible meanings are: "UNDEF", "INCR_VALUE", "ABS_VALUE", "SHORT_WAY", "POS_ROT_DIR", "NEG_ROT_DIR"
    # Ext2dCircleMeaning
    def AddAttributes(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def Ext2dCircleMeaningStart(builder): builder.StartObject(2)
def Start(builder):
    return Ext2dCircleMeaningStart(builder)
def Ext2dCircleMeaningAddMeaning(builder, meaning): builder.PrependInt8Slot(0, meaning, 0)
def AddMeaning(builder, meaning):
    return Ext2dCircleMeaningAddMeaning(builder, meaning)
def Ext2dCircleMeaningAddAddAttributes(builder, addAttributes): builder.PrependInt8Slot(1, addAttributes, 0)
def AddAddAttributes(builder, addAttributes):
    return Ext2dCircleMeaningAddAddAttributes(builder, addAttributes)
def Ext2dCircleMeaningEnd(builder): return builder.EndObject()
def End(builder):
    return Ext2dCircleMeaningEnd(builder)

class Ext2dCircleMeaningT(object):

    # Ext2dCircleMeaningT
    def __init__(self):
        self.meaning = 0  # type: int
        self.addAttributes = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        ext2dCircleMeaning = Ext2dCircleMeaning()
        ext2dCircleMeaning.Init(buf, pos)
        return cls.InitFromObj(ext2dCircleMeaning)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, ext2dCircleMeaning):
        x = Ext2dCircleMeaningT()
        x._UnPack(ext2dCircleMeaning)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.meaning == other.meaning and \
            self.addAttributes == other.addAttributes

    # Ext2dCircleMeaningT
    def _UnPack(self, ext2dCircleMeaning):
        if ext2dCircleMeaning is None:
            return
        self.meaning = ext2dCircleMeaning.Meaning()
        self.addAttributes = ext2dCircleMeaning.AddAttributes()

    # Ext2dCircleMeaningT
    def Pack(self, builder):
        Ext2dCircleMeaningStart(builder)
        Ext2dCircleMeaningAddMeaning(builder, self.meaning)
        Ext2dCircleMeaningAddAddAttributes(builder, self.addAttributes)
        ext2dCircleMeaning = Ext2dCircleMeaningEnd(builder)
        return ext2dCircleMeaning
