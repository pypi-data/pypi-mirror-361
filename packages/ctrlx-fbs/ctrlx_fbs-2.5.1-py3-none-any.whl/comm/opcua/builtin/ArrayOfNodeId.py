# automatically generated by the FlatBuffers compiler, do not modify

# namespace: builtin

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ArrayOfNodeId(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayOfNodeId()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayOfNodeId(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ArrayOfNodeId
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ArrayOfNodeId
    def Array(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from comm.opcua.builtin.NodeId import NodeId
            obj = NodeId()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ArrayOfNodeId
    def ArrayLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ArrayOfNodeId
    def ArrayIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ArrayOfNodeIdStart(builder): builder.StartObject(1)
def Start(builder):
    return ArrayOfNodeIdStart(builder)
def ArrayOfNodeIdAddArray(builder, array): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(array), 0)
def AddArray(builder, array):
    return ArrayOfNodeIdAddArray(builder, array)
def ArrayOfNodeIdStartArrayVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartArrayVector(builder, numElems):
    return ArrayOfNodeIdStartArrayVector(builder, numElems)
def ArrayOfNodeIdEnd(builder): return builder.EndObject()
def End(builder):
    return ArrayOfNodeIdEnd(builder)
import comm.opcua.builtin.NodeId
try:
    from typing import List
except:
    pass

class ArrayOfNodeIdT(object):

    # ArrayOfNodeIdT
    def __init__(self):
        self.array = None  # type: List[comm.opcua.builtin.NodeId.NodeIdT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayOfNodeId = ArrayOfNodeId()
        arrayOfNodeId.Init(buf, pos)
        return cls.InitFromObj(arrayOfNodeId)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayOfNodeId):
        x = ArrayOfNodeIdT()
        x._UnPack(arrayOfNodeId)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.array == other.array

    # ArrayOfNodeIdT
    def _UnPack(self, arrayOfNodeId):
        if arrayOfNodeId is None:
            return
        if not arrayOfNodeId.ArrayIsNone():
            self.array = []
            for i in range(arrayOfNodeId.ArrayLength()):
                if arrayOfNodeId.Array(i) is None:
                    self.array.append(None)
                else:
                    nodeId_ = comm.opcua.builtin.NodeId.NodeIdT.InitFromObj(arrayOfNodeId.Array(i))
                    self.array.append(nodeId_)

    # ArrayOfNodeIdT
    def Pack(self, builder):
        if self.array is not None:
            arraylist = []
            for i in range(len(self.array)):
                arraylist.append(self.array[i].Pack(builder))
            ArrayOfNodeIdStartArrayVector(builder, len(self.array))
            for i in reversed(range(len(self.array))):
                builder.PrependUOffsetTRelative(arraylist[i])
            array = builder.EndVector()
        ArrayOfNodeIdStart(builder)
        if self.array is not None:
            ArrayOfNodeIdAddArray(builder, array)
        arrayOfNodeId = ArrayOfNodeIdEnd(builder)
        return arrayOfNodeId
