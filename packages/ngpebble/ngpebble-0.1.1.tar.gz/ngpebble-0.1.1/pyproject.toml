[project]
name = "ngpebble"
version = "0.1.1"
description = "Ngrok-like tunnel via WebSocket"
authors = [{name = "Eshan Das"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "websockets",
    "requests"
]

[project.scripts]
ngpebble = "ngpebble.__main__:main"
