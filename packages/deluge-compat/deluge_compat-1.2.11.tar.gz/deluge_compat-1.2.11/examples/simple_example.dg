// Simple Deluge script example
response = Map();
name = "World";
message = "Hello " + name + "!";
response.put("greeting", message);
response.put("timestamp", "2024-01-01");

// Test string operations
text = "Sample Text";
upperText = text.toUpperCase();
length = text.length();

info "Message: " + message;
info "Upper text: " + upperText;
info "Length: " + length;

return response;