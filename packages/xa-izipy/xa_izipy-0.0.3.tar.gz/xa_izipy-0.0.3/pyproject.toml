[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xa_izipy"
version = "0.0.3"
description = "Упрощает какие-то функции"   # кратко про библиотеку
readme = "README.md"                        # файл с описанием (обязательно создай его)
requires-python = ">=3.6"                   # минимальная версия Python
authors = [
  {name = "Rigew"}                          # автор (можно добавить email)
]
dependencies = [
  "psutil"
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent"
]
